[build-system]
requires = ["setuptools >= 77.0.3"]
requires-python = ">=3.12"
build-backend = "setuptools.build_meta"

[project]
name = "dmcp"
dynamic = ["version"]
dependencies = [
    "fastmcp>=2.10.4",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
